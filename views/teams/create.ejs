<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Team - TheGuysofeFootball</title>
    <link href="/dist/style.css" rel="stylesheet">
</head>
<body class="min-h-screen flex flex-col bg-gray-50">
    <%- include('../partials/nav') %>
    
    <main class="flex-1 py-10">
        <div class="container mx-auto px-4">
            <!-- Form Header -->
            <div class="max-w-2xl mx-auto bg-gradient-to-r from-blue-900 to-blue-800 rounded-t-xl p-8 text-center">
                <h1 class="text-3xl font-bold text-white mb-3">Create New Team</h1>
                <p class="text-lg text-gray-200">Register your team for tournaments</p>
            </div>

            <!-- Form Content -->
            <div class="max-w-2xl mx-auto bg-white rounded-b-xl shadow-lg p-8">
                <% if (locals.error) { %>
                    <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6">
                        <p class="text-red-700"><%= error %></p>
                    </div>
                <% } %>

                <form action="/teams/create" method="POST" class="space-y-6">
                    <div class="space-y-2">
                        <label for="name" class="block text-sm font-semibold text-gray-700">Team Name</label>
                        <input type="text" 
                               name="name" 
                               id="name" 
                               required
                               value="<%= locals.formData ? formData.name : '' %>"
                               class="block w-full px-4 py-3 text-gray-900 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent bg-white shadow-sm transition duration-150 ease-in-out"
                               placeholder="Enter team name">
                    </div>

                    <div class="flex justify-end space-x-4 pt-6">
                        <a href="/teams/list" 
                           class="px-6 py-3 text-sm font-medium text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-lg transition duration-150 ease-in-out flex items-center">
                            Cancel
                        </a>
                        <button type="submit" 
                                class="px-6 py-3 text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 rounded-lg transition duration-150 ease-in-out flex items-center">
                            Create Team
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>