<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= tournament.name %> - TheGuysofeFootball</title>
    <link href="/dist/style.css" rel="stylesheet">
</head>
<body class="min-h-screen flex flex-col bg-gray-50">
    <%- include('../partials/nav') %>
    
    <main class="flex-1 py-10">
        <div class="container mx-auto px-4">
            <div class="max-w-4xl mx-auto bg-white rounded-lg shadow-md p-6">
                <h1 class="text-3xl font-bold text-gray-900 mb-4"><%= tournament.name %></h1>
                <div class="grid grid-cols-2 gap-4 mb-6">
                    <div>
                        <p class="text-sm text-gray-500">Format</p>
                        <p class="text-lg font-medium"><%= tournament.format %></p>
                    </div>
                    <div>
                        <p class="text-sm text-gray-500">Status</p>
                        <p class="text-lg font-medium"><%= tournament.status %></p>
                    </div>
                </div>
                <div class="prose max-w-none">
                    <p><%= tournament.description %></p>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>