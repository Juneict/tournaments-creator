<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Tournament - TheGuysofeFootball</title>
    <link href="/dist/style.css" rel="stylesheet">
</head>
<body class="min-h-screen flex flex-col bg-gray-50">
    <%- include('../partials/nav') %>
    
    <main class="flex-1 py-10">
        <div class="container mx-auto px-4">
            <div class="max-w-3xl mx-auto">
                <div class="bg-white shadow-lg rounded-lg p-8">
                    <h1 class="text-3xl font-bold text-gray-900 mb-8">Create Tournament</h1>

                    <% if (locals.error) { %>
                        <div class="bg-red-50 border-l-4 border-red-400 p-4 mb-6">
                            <p class="text-sm text-red-700"><%= error %></p>
                        </div>
                    <% } %>

                    <form action="/tournaments/create" method="POST" class="space-y-6">
                        <!-- Tournament Name Input -->
                        <div class="form-group">
                            <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Tournament Name</label>
                            <div class="relative">
                                <input type="text" 
                                       id="name" 
                                       name="name" 
                                       required
                                       placeholder="Enter tournament name"
                                       value="<%= locals.formData ? formData.name : '' %>"
                                       class="mt-1 block w-full px-4 py-3 rounded-lg border-2 border-gray-200 focus:border-blue-500 focus:ring focus:ring-blue-200 focus:ring-opacity-50 transition-colors duration-200">
                            </div>
                        </div>

                        <!-- Format Select -->
                        <div class="form-group">
                            <label for="format" class="block text-sm font-medium text-gray-700 mb-1">Tournament Format</label>
                            <div class="relative">
                                <select id="format" 
                                        name="format" 
                                        required
                                        class="mt-1 block w-full px-4 py-3 rounded-lg border-2 border-gray-200 focus:border-blue-500 focus:ring focus:ring-blue-200 focus:ring-opacity-50 transition-colors duration-200 bg-white">
                                    <option value="" disabled selected>Select tournament format</option>
                                    <option value="league" <%= locals.formData && formData.format === 'league' ? 'selected' : '' %>>League</option>
                                    <option value="knockout" <%= locals.formData && formData.format === 'knockout' ? 'selected' : '' %>>Knockout</option>
                                </select>
                                <div class="absolute inset-y-0 right-0 flex items-center px-2 pointer-events-none">
                                    <svg class="h-4 w-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                                    </svg>
                                </div>
                            </div>
                        </div>

                        <!-- Maximum Teams Input -->
                        <div class="form-group">
                            <label for="maxTeams" class="block text-sm font-medium text-gray-700 mb-1">Maximum Teams</label>
                            <div class="relative">
                                <input type="number" 
                                       id="maxTeams" 
                                       name="maxTeams" 
                                       required
                                       min="2"
                                       max="32"
                                       placeholder="Enter number of teams"
                                       value="<%= locals.formData ? formData.maxTeams : '8' %>"
                                       class="mt-1 block w-full px-4 py-3 rounded-lg border-2 border-gray-200 focus:border-blue-500 focus:ring focus:ring-blue-200 focus:ring-opacity-50 transition-colors duration-200">
                                <div class="absolute inset-y-0 right-0 flex items-center pr-8">
                                    <span class="text-gray-500 text-sm">teams</span>
                                </div>
                            </div>
                            <p class="mt-1 text-sm text-gray-500">Minimum 2, maximum 32 teams</p>
                        </div>

                        <!-- Description Textarea -->
                        <div class="form-group">
                            <label for="description" class="block text-sm font-medium text-gray-700 mb-1">Tournament Description</label>
                            <textarea id="description" 
                                      name="description" 
                                      rows="4"
                                      placeholder="Enter tournament description"
                                      class="mt-1 block w-full px-4 py-3 rounded-lg border-2 border-gray-200 focus:border-blue-500 focus:ring focus:ring-blue-200 focus:ring-opacity-50 transition-colors duration-200 resize-none"><%= locals.formData ? formData.description : '' %></textarea>
                        </div>

                        <!-- Submit Button -->
                        <div class="pt-6">
                            <button type="submit" 
                                    class="w-full flex justify-center py-3 px-4 border-0 rounded-lg text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200 shadow-lg hover:shadow-xl">
                                Create Tournament
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>